export const environment = {
  production: false,
  // npm start and npm run start:hosted use the hosted API. start:local uses the local proxy.
  apiBaseUrl: 'https://premium-shop-backend.onrender.com'
};
